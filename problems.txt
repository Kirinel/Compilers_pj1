1. numbers may be double or integer, the output accuracy
2. an exp should be after RETURN, but did not output "exp:\n"
3. the different output of "type" and ret_type
//4. vdecls should output at the end of func
5. there's only 1 funcs in the example and no grammar rules for funcs
6. "var" with different tags
7. "params" with funccall should be after the globid
8. statement "assign" needs to output vdecl?

1. expstmt
2. type ref int
3. '-' between each statements (only in run() function)
4. redundant output "name: varval" in vdecl
5. "exps" in if statement
6. details about stmt with name "if"